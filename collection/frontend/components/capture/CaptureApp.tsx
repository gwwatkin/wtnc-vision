/**
 * CaptureApp.tsx — Root capture-page component.
 * STUB: task6 fills the real implementation.
 *
 * @module components/capture/CaptureApp
 */

import type { CaptureAppProps } from '../../types';

export function CaptureApp(props: CaptureAppProps) {
  return <div />;
}
