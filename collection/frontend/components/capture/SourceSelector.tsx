/**
 * SourceSelector.tsx — Source selector component.
 * STUB: task6 fills the real implementation.
 *
 * @module components/capture/SourceSelector
 */

import type { SourceSelectorProps } from '../../types';

export function SourceSelector(props: SourceSelectorProps) {
  return <div />;
}
