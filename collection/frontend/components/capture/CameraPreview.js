/**
 * CameraPreview.js — Live camera preview; manages its own getUserMedia stream.
 * Stub body filled by task6.
 *
 * @module components/capture/CameraPreview
 */

import { html } from '../../vendor/preact-setup.js';

/**
 * @param {import('../../types').CameraPreviewProps} props
 * @returns {any}
 */
export function CameraPreview(props) {
  return html``;
}
