/**
 * CaptureControls.js — Start/stop capture controls with inflight counter.
 * Stub body filled by task6.
 *
 * @module components/capture/CaptureControls
 */

import { html } from '../../vendor/preact-setup.js';

/**
 * @param {import('../../types').CaptureControlsProps} props
 * @returns {any}
 */
export function CaptureControls(props) {
  return html``;
}
