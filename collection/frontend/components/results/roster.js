/**
 * roster.js — Populates the shared #roster-numbers datalist (FROZEN-8).
 * Stub body filled by task4.
 *
 * @module components/results/roster
 */

/**
 * Fetches the roster for the given run and writes <option> elements to the
 * shared <datalist id="roster-numbers"> in index.html.
 * @param {string} run
 * @returns {Promise<void>}
 */
export async function setRosterOptions(run) { throw new Error('stub'); }
