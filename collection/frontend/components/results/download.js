/**
 * download.js — Stub. Task2 fills the full implementation.
 *
 * Provides the exact named export that task6 (ResultsApp) will call.
 * DOM anchor lives here (not in api.js) per design §6.2.
 *
 * @module components/results/download
 */

/**
 * Download the crossings for a run in the given format.
 * Fetches a Blob via api.fetchExportBlob then triggers a named file download.
 *
 * @param {string} _run
 * @param {'csv'|'json'} _format
 * @returns {Promise<void>}
 */
export async function downloadResults(_run, _format) { /* task2 fills */ }
