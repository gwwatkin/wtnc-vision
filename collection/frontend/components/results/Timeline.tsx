/**
 * Timeline.tsx — Timeline, Pack, and GapSeparator components.
 * STUB: task3 fills the real implementation.
 *
 * @module components/results/Timeline
 */

import type { TimelineProps, PackProps, GapSeparatorProps } from '../../types';

export function Timeline(props: TimelineProps) {
  return <div />;
}

export function Pack(props: PackProps) {
  return <div />;
}

export function GapSeparator(props: GapSeparatorProps) {
  return <div />;
}
